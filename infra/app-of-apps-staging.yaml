apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/chowieuk/chowie-dev-config.git
    targetRevision: HEAD
    path: infra/apps-staging
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
