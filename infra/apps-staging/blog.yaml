apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blog-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/chowieuk/chowie-dev-config.git
    targetRevision: HEAD
    path: apps/blog/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: blog-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
